# Block all direct web access to the database and logs in this directory.
# Apache 2.4 / LiteSpeed:
Require all denied

# Apache 2.2 fallback:
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
